# pal Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Browse every palette from `~/.config/pal/config.toml` as a native Raycast view, with icons, accessories, detail panes and action panels.
- Run items through `pal pick`, including the result envelope: toasts, HUDs, clipboard writes, opened URLs, reloads and palette hand-offs.
- Bake a palette's items into script commands so they are reachable from root search, refreshed on a schedule.
