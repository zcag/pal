{
  "$defs": {
    "BadgeStyle": {
      "description": "`badge_style`: how a count badge is drawn.",
      "oneOf": [
        {
          "const": "count",
          "description": "The number (` ·3` on the menu bar, in red on sketchybar's label).",
          "type": "string"
        },
        {
          "const": "dot",
          "description": "A dot, whatever the number.",
          "type": "string"
        },
        {
          "const": "none",
          "description": "Nothing; the count stays in the tooltip.",
          "type": "string"
        }
      ]
    },
    "Bar": {
      "additionalProperties": false,
      "description": "`[bar]`: the strips extensions draw on (`docs/design/bar.md`), the\nhover timings, one table per target and one per item.",
      "properties": {
        "hover_delay": {
          "default": 250,
          "description": "Milliseconds the pointer rests on an item before a peek opens.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "hover_grace": {
          "default": 400,
          "description": "Milliseconds after the pointer has left both the item and the\npopover before a peek closes.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "items": {
          "additionalProperties": {
            "$ref": "#/$defs/BarItemConfig"
          },
          "default": {},
          "description": "Per-item settings, keyed `extension/id` (`[bar.items.\"github/notifications\"]`).",
          "type": "object"
        },
        "menubar": {
          "$ref": "#/$defs/BarMenubar",
          "default": {
            "badge_style": "count",
            "dim": 50,
            "font": "system",
            "icon_size": 0.0,
            "max_chars": 32,
            "opacity": 100,
            "open_on_hover": false,
            "show_icon": true,
            "show_title": true,
            "size": 0.0,
            "spacing": 4,
            "text_size": 0.0,
            "urgent_color": "destructive",
            "width": 0
          }
        },
        "sketchybar": {
          "$ref": "#/$defs/BarSketchybar",
          "default": {
            "badge_style": "count",
            "dim": 50,
            "font": "system",
            "icon_size": 0.0,
            "max_chars": 32,
            "opacity": 100,
            "open_on_hover": false,
            "position": "right",
            "show_icon": true,
            "show_title": true,
            "size": 0.0,
            "spacing": 4,
            "text_size": 0.0,
            "urgent_color": "destructive",
            "width": 0
          }
        },
        "target": {
          "$ref": "#/$defs/BarTarget",
          "default": "auto",
          "description": "Default target for every item; an item's own `target` overrides."
        }
      },
      "type": "object"
    },
    "BarFont": {
      "description": "`font`: the text's face.",
      "oneOf": [
        {
          "const": "system",
          "description": "The bar's own face.",
          "type": "string"
        },
        {
          "const": "mono",
          "description": "Monospace, for codes and times that must not jitter (SF Mono on the menu bar, Menlo on sketchybar).",
          "type": "string"
        }
      ]
    },
    "BarItemConfig": {
      "additionalProperties": false,
      "description": "`[bar.items.\"<extension>/<id>\"]`: one item's settings; absent keys mean\nthe target's defaults.",
      "properties": {
        "badge_color": {
          "description": "The colour of this item's count badge and dot, a name or `#rrggbb`. The target's when unset (its own colour when that is too).",
          "type": "string"
        },
        "badge_style": {
          "anyOf": [
            {
              "$ref": "#/$defs/BadgeStyle"
            },
            {
              "type": "null"
            }
          ],
          "description": "How this item's count badge is drawn. The target's when unset."
        },
        "color": {
          "description": "This item's tint, a colour name or `#rrggbb`. The target's when unset.",
          "type": "string"
        },
        "dim": {
          "description": "A muted item's strength here, percent. The target's when unset.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": true,
          "description": "`false`: no slot on any target and no refresh timer.",
          "type": "boolean"
        },
        "font": {
          "anyOf": [
            {
              "$ref": "#/$defs/BarFont"
            },
            {
              "type": "null"
            }
          ],
          "description": "`system` or `mono` for this item. The target's when unset."
        },
        "hide_when": {
          "description": "The opposite: the item leaves the strip while this is true. Both\nmay be set.",
          "type": "string"
        },
        "hotkey": {
          "description": "Global hotkey that opens the item's popover engaged (or runs its open action).",
          "type": "string"
        },
        "icon": {
          "description": "A glyph, an emoji or a short text drawn as this item's icon in place of the extension's. The extension's when unset.",
          "type": "string"
        },
        "icon_size": {
          "description": "Point size of this item's glyph alone; `0` follows `size`. The target's when unset.",
          "format": "double",
          "type": "number"
        },
        "max_chars": {
          "description": "This item's longest title, in characters. The target's when unset.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "opacity": {
          "description": "This item's strength, percent, on every colour it draws. The target's when unset.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "open_on_hover": {
          "description": "A hover peeks this item; the target's default when unset.",
          "type": "boolean"
        },
        "order": {
          "description": "Order among pal's own items: ascending left to right on the menu\nbar and within a sketchybar position.",
          "format": "int64",
          "type": "integer"
        },
        "position": {
          "description": "sketchybar position for this item (as `[bar.sketchybar] position`).",
          "type": "string"
        },
        "show": {
          "$ref": "#/$defs/BarShow",
          "default": "auto",
          "description": "`always` keeps the item on the strip when it has nothing to say, as\nthe muted quiet shape its render offers; `auto` hides it then."
        },
        "show_icon": {
          "description": "Draw this item's icon. The target's when unset.",
          "type": "boolean"
        },
        "show_title": {
          "description": "Draw this item's title and segments. The target's when unset.",
          "type": "boolean"
        },
        "show_when": {
          "description": "A state expression (`working`, `hour >= 9 and not deep`): the item\nis on the strip only while it is true. Read at draw time; no render\nwhile hidden.",
          "type": "string"
        },
        "size": {
          "description": "Point size of the glyph and the text here. The target's when unset.",
          "format": "double",
          "type": "number"
        },
        "spacing": {
          "description": "Points between the icon, the title and the segments here. The target's when unset.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "target": {
          "anyOf": [
            {
              "$ref": "#/$defs/BarTarget"
            },
            {
              "type": "null"
            }
          ],
          "description": "This item's target; the global `[bar] target` when unset."
        },
        "text_size": {
          "description": "Point size of this item's text alone; `0` follows `size`. The target's when unset.",
          "format": "double",
          "type": "number"
        },
        "urgent_color": {
          "description": "This item's colour when urgent. The target's when unset.",
          "type": "string"
        },
        "width": {
          "description": "A fixed width in points for this item; `0` is natural. The target's when unset.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "BarMenubar": {
      "additionalProperties": false,
      "description": "`[bar.menubar]`: the macOS menu bar target. Hover off: Apple's bar has no hover convention.",
      "properties": {
        "badge_color": {
          "description": "The colour of a count badge and a dot, a name or `#rrggbb`. Unset\ndraws them in the item's own colour (the tint; `urgent_color` while\nurgent), so a mail count is not red by default.",
          "type": "string"
        },
        "badge_style": {
          "$ref": "#/$defs/BadgeStyle",
          "default": "count",
          "description": "How a count badge is drawn: `count`, `dot` or `none`."
        },
        "color": {
          "description": "The tint: a colour name (`grey`, `blue`, `green`, `amber`, `red`,\n`violet`, `pink`, `teal`, `accent`, `text`, `muted`) or `#rrggbb`,\ndrawn in place of the colour the extension answers. Unset keeps the\nextension's: a coloured item its own, the rest the bar's text colour.",
          "type": "string"
        },
        "dim": {
          "default": 50,
          "description": "A muted item's strength, in percent: a stale item and an item the\nextension colours `muted` draw at this opacity.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "font": {
          "$ref": "#/$defs/BarFont",
          "default": "system",
          "description": "`system` or `mono`."
        },
        "icon": {
          "description": "A glyph, an emoji or a short text drawn as the icon in place of the\none the extension answers (what `BarItem.icon` takes as a string).\nOnly an item's own makes sense (every item the same icon is no\nbar), so a target leaves it unset.",
          "type": "string"
        },
        "icon_size": {
          "default": 0.0,
          "description": "Point size of the glyph alone; `0` follows `size`.",
          "format": "double",
          "type": "number"
        },
        "max_chars": {
          "default": 32,
          "description": "Longest title an item draws, in characters; longer text ends in an\nellipsis. Apple's bar hides whatever runs into the notch or past the\nleft edge, so a lyric line or a track title must stop short.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "opacity": {
          "default": 100,
          "description": "The item's strength, in percent: every colour it draws (the icon,\nthe text, the segments, the badge) at this alpha. A muted item is\nat `dim` of this (the two multiply: `opacity = 60`, `dim = 50` draws\na stale item at 30).",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "open_on_hover": {
          "default": false,
          "description": "A hover peeks the item's popover. Off: Apple's bar has no hover convention.",
          "type": "boolean"
        },
        "show_icon": {
          "default": true,
          "description": "Draw the icon.",
          "type": "boolean"
        },
        "show_title": {
          "default": true,
          "description": "Draw the title and the segments; off is a glyph-only item.",
          "type": "boolean"
        },
        "size": {
          "default": 0.0,
          "description": "Point size of the glyph and the text; `0` is the target's own (the\nmenu bar's 13 pt text and 14 pt glyph, sketchybar's font).",
          "format": "double",
          "type": "number"
        },
        "spacing": {
          "default": 4,
          "description": "Points between the icon, the title and the segments.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "text_size": {
          "default": 0.0,
          "description": "Point size of the title and the segments alone; `0` follows `size`.",
          "format": "double",
          "type": "number"
        },
        "urgent_color": {
          "default": "destructive",
          "description": "The colour of an urgent item, a name or `#rrggbb`.",
          "type": "string"
        },
        "width": {
          "default": 0,
          "description": "A fixed width in points, so a ticking timer does not jitter; `0`\nis the natural width. Text past it is clipped.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "BarShow": {
      "description": "`show`: what an item with nothing to say does with its slot.",
      "oneOf": [
        {
          "const": "auto",
          "description": "Off the strip while `render` answers `hidden` (the rule: an item earns its slot).",
          "type": "string"
        },
        {
          "const": "always",
          "description": "Kept on the strip as the quiet shape its render offers (`BarItem.empty`:\nthe glyph, an honest tooltip, the same popover), muted, without a badge.\nAn item whose render offers none hides either way.",
          "type": "string"
        }
      ]
    },
    "BarSketchybar": {
      "additionalProperties": false,
      "description": "`[bar.sketchybar]`: the sketchybar target. Hover off too: a popover opens on a click, its hotkey, or a hover the owner asks for.",
      "properties": {
        "badge_color": {
          "description": "The colour of a count badge and a dot, a name or `#rrggbb`. Unset\ndraws them in the item's own colour (the tint; `urgent_color` while\nurgent), so a mail count is not red by default.",
          "type": "string"
        },
        "badge_style": {
          "$ref": "#/$defs/BadgeStyle",
          "default": "count",
          "description": "How a count badge is drawn: `count`, `dot` or `none`."
        },
        "color": {
          "description": "The tint: a colour name (`grey`, `blue`, `green`, `amber`, `red`,\n`violet`, `pink`, `teal`, `accent`, `text`, `muted`) or `#rrggbb`,\ndrawn in place of the colour the extension answers. Unset keeps the\nextension's: a coloured item its own, the rest the bar's text colour.",
          "type": "string"
        },
        "colors": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Overrides of the colour map, `0xAARRGGBB` per name (`red`,\n`muted`, `text`, ...), so a themed bar keeps its own palette.",
          "type": "object"
        },
        "dim": {
          "default": 50,
          "description": "A muted item's strength, in percent: a stale item and an item the\nextension colours `muted` draw at this opacity.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "font": {
          "$ref": "#/$defs/BarFont",
          "default": "system",
          "description": "`system` or `mono`."
        },
        "icon": {
          "description": "A glyph, an emoji or a short text drawn as the icon in place of the\none the extension answers (what `BarItem.icon` takes as a string).\nOnly an item's own makes sense (every item the same icon is no\nbar), so a target leaves it unset.",
          "type": "string"
        },
        "icon_size": {
          "default": 0.0,
          "description": "Point size of the glyph alone; `0` follows `size`.",
          "format": "double",
          "type": "number"
        },
        "max_chars": {
          "default": 32,
          "description": "Longest title an item draws, in characters; longer text ends in an\nellipsis. Apple's bar hides whatever runs into the notch or past the\nleft edge, so a lyric line or a track title must stop short.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "opacity": {
          "default": 100,
          "description": "The item's strength, in percent: every colour it draws (the icon,\nthe text, the segments, the badge) at this alpha. A muted item is\nat `dim` of this (the two multiply: `opacity = 60`, `dim = 50` draws\na stale item at 30).",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "open_on_hover": {
          "default": false,
          "description": "A hover peeks the item's popover. Off: a click opens and closes, like the menu bar; the owner's sketchybar may turn it on.",
          "type": "boolean"
        },
        "position": {
          "default": "right",
          "description": "Default position of pal's items: `left`, `right`, `center`, `q`,\n`e`, `before:<item>` or `after:<item>`.",
          "type": "string"
        },
        "show_icon": {
          "default": true,
          "description": "Draw the icon.",
          "type": "boolean"
        },
        "show_title": {
          "default": true,
          "description": "Draw the title and the segments; off is a glyph-only item.",
          "type": "boolean"
        },
        "size": {
          "default": 0.0,
          "description": "Point size of the glyph and the text; `0` is the target's own (the\nmenu bar's 13 pt text and 14 pt glyph, sketchybar's font).",
          "format": "double",
          "type": "number"
        },
        "spacing": {
          "default": 4,
          "description": "Points between the icon, the title and the segments.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "text_size": {
          "default": 0.0,
          "description": "Point size of the title and the segments alone; `0` follows `size`.",
          "format": "double",
          "type": "number"
        },
        "urgent_color": {
          "default": "destructive",
          "description": "The colour of an urgent item, a name or `#rrggbb`.",
          "type": "string"
        },
        "width": {
          "default": 0,
          "description": "A fixed width in points, so a ticking timer does not jitter; `0`\nis the natural width. Text past it is clipped.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "BarTarget": {
      "description": "Where bar items are drawn. `auto`: sketchybar when it answers\n(`sketchybar --query bar`), else the macOS menu bar.",
      "enum": [
        "auto",
        "menubar",
        "sketchybar",
        "both",
        "off"
      ],
      "type": "string"
    },
    "Caps": {
      "additionalProperties": false,
      "description": "How many rows one source may put in a typed query's answer, by tier.\nThe rest of its matches are counted in [`Ranked::more`], for a row that\ndrills into the palette (where nothing is capped). `[general]\nroot_caps` in the config file. Before the cap, a source with a row that\nhas the typed word loses its rows that only scatter the letters (the\nsame `more` row keeps them reachable): `spo` is Spotify, not Spotify and\nsix apps that spell it out of their bundle ids; `sp` still lists every\napp whose name starts with it, up to the cap, so fuzzy typing is intact.",
      "properties": {
        "catalog": {
          "default": 3,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "normal": {
          "default": 6,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "primary": {
          "default": 8,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "Confirm": {
      "anyOf": [
        {
          "description": "`deeplink_confirm = true` (ask) or `false` (never).",
          "type": "boolean"
        },
        {
          "description": "`deeplink_confirm = [\"timer\", \"quicklinks\"]`: ask, except for these.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      ],
      "description": "`general.deeplink_confirm`: whether a link that acts shows the confirm\ncard. `true`/`false` for all, or the extensions whose links are trusted\n(the card is skipped for `run`, `form` and routes into them)."
    },
    "Decl": {
      "additionalProperties": false,
      "description": "One `[states.<name>]` table.",
      "properties": {
        "default": {
          "description": "The value with nothing set and no expression; `false` when absent."
        },
        "description": {
          "description": "What the States palette shows under the name.",
          "type": "string"
        },
        "expr": {
          "description": "A Jinja expression over other states (`hour >= 9 and hour < 18`);\nre-evaluated whenever a state it names changes. Without one the\nstate is only ever set by hand, from the CLI or by a publisher.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "General": {
      "additionalProperties": false,
      "properties": {
        "alias_space": {
          "default": true,
          "description": "Typing a palette's alias (or its name, or its one-word title) and a\nspace at the root jumps into that palette with the rest typed:\n`calc 2+2`, `emoji cat`. `false` leaves the space as a character.",
          "type": "boolean"
        },
        "ask_permissions_on_start": {
          "default": true,
          "description": "macOS: ask for the Accessibility permission (the system prompt, and\nSystem Settings on that pane) the first time the panel shows on a\nprofile that has not hidden the Welcome tips yet. Paste and window\nswitching need it; `false` leaves the ask to the Welcome row and to\nSettings > General > Permissions.",
          "type": "boolean"
        },
        "backspace_back": {
          "default": true,
          "description": "Backspace with nothing typed goes back a level, the way `cmd+backspace`\ndoes (never at the root; a row's own Backspace action, a folder's\nGo up, comes first; a view's keys are its own). `false` leaves it\nto the row alone.",
          "type": "boolean"
        },
        "check_updates": {
          "default": true,
          "description": "Look for a newer release 20 s after startup and once a day, in\nrelease builds (the GitHub release manifest; nothing is downloaded\nby the check). A found release shows on the Overview, the About\npage and as an \"Install Update\" row at the root; installing is\nalways your click. `false` turns the automatic check off; \"Check\nfor Updates\" still runs one on demand.",
          "type": "boolean"
        },
        "compact": {
          "default": false,
          "description": "Compact mode: the panel 560 px wide with 32 px rows, no detail\npane, and the footer folded into the search row (the primary\naction's hint on its right). `cmd+shift+m` in the panel flips it\nand writes it here, so it is remembered per profile.",
          "type": "boolean"
        },
        "deeplink_confirm": {
          "$ref": "#/$defs/Confirm",
          "default": true,
          "description": "A `pal://` link that acts (`run`, `form`, `paste`, `open?url=`, an\nextension's route; a web page can emit one) shows a confirm card\nfirst. `true` asks; `false` runs it straight away, for scripts that\ndrive pal by link; a list of extension names asks for everything\nexcept links into those extensions (a name covers every instance of\nit, a key one instance). `install`, `update`, `remove` and\n`instance/*` always ask; the CLI never does (docs/design/links.md)."
        },
        "extension_dirs": {
          "default": [],
          "description": "Extra directories of extensions (one subdirectory per extension,\nlike the store), for a dotfiles-managed set. Loaded after the\nbundled extensions and the store, so a name in a later directory\nreplaces an earlier one. `~` is expanded. The host is restarted\n(`pal reload`) before a change here is seen.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fallbacks": {
          "default": [
            "web",
            "url",
            "quicklinks",
            "calc",
            "files"
          ],
          "description": "The rows offered when a typed query matches nothing, in this order:\n`web` (Search the web, `search_engine`), `url` (Open as URL, when the\nquery looks like one), then palette ids that opted in (`quicklinks`\nfills its `{query}` links, `calc` and `files` open with the query\ntyped, any other input palette as \"Ask <name>\"). A fallback palette\nnot named here comes after these, in load order; one named here that\ndoes not exist is skipped.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fallbacks_always": {
          "default": false,
          "description": "Show the fallback rows under the hits as well, not only when nothing\nmatched.",
          "type": "boolean"
        },
        "hotkey": {
          "$ref": "#/$defs/Hotkeys",
          "default": "ctrl+space",
          "description": "Global hotkey that shows pal, e.g. `ctrl+space`, or several that\nall do (`[\"cmd+space\", \"ctrl+space\"]`). Empty turns it off, for a\ncompositor keybind that runs `pal toggle` instead. On macOS\n`cmd+space` is Spotlight's until its \"Show Spotlight search\"\nshortcut is unticked under System Settings > Keyboard > Keyboard\nShortcuts; pal says so in Settings and registers it once it is free."
        },
        "launch_at_login": {
          "default": false,
          "description": "Start pal when you sign in: a LaunchAgent on macOS, a `pal.service`\nuser unit on Linux (an XDG autostart entry without systemd). The\nsame agent relaunches pal after a crash, on or off.",
          "type": "boolean"
        },
        "menu_bar_icon": {
          "default": true,
          "description": "Show pal's icon in the menu bar (macOS) or system tray (Linux). The\napp has no Dock icon, so this is the visible way to reach Settings\nand Quit; the hotkey and `pal settings` work without it.",
          "type": "boolean"
        },
        "now": {
          "default": [
            "calendar-today",
            "timer-timers",
            "media",
            "clipboard-rows"
          ],
          "description": "The palettes whose `suggest()` rows lead the empty root (\"Now\"), in\nthis order; suggesting palettes not named here follow in load order.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "pop_to_root": {
          "default": "after 90s",
          "description": "What a re-show lands on: `\"always\"` back at the root (as before),\n`\"never\"` where you left (the level and the query kept), or `\"after\n90s\"`: kept while the panel was hidden for less than that many\nseconds, the root after. A palette hotkey always opens its palette.",
          "type": "string"
        },
        "position": {
          "$ref": "#/$defs/Position",
          "default": "top",
          "description": "Where the panel appears on the screen with the pointer."
        },
        "root_caps": {
          "$ref": "#/$defs/Caps",
          "default": {
            "catalog": 3,
            "normal": 6,
            "primary": 8
          },
          "description": "How many rows one palette may show at the root for a typed query,\nby its tier: `{ primary = 8, normal = 6, catalog = 3 }`. The rest\nis a \"N more in ...\" row that opens the palette. The empty query\nand a palette's own level are never capped."
        },
        "root_cut": {
          "default": true,
          "description": "At the root, a palette with a row that has the typed word shows\nonly those rows: `spo` is Spotify, not Spotify and six apps that\nspell it out of their bundle ids (they stay in the \"N more\" row).\n`false` leaves every match to the cap.",
          "type": "boolean"
        },
        "root_first": {
          "default": [
            "browser-tabs/tabs",
            "windows/windows",
            "apps/apps",
            "pal/palettes"
          ],
          "description": "Palettes whose rows lead the others of their band at the root, in\nthis order: `[\"browser-tabs/tabs\", \"windows/windows\", \"apps/apps\",\n\"pal/palettes\"]` (a tab over a window over an app over a palette\nrow, all over the rest). Each gets\n`root_first_step` more than the next, on top of its tier, which\norders rows of equal standing and never lifts a row that only\nscatters the letters over one that has the typed word.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "root_first_step": {
          "default": 30.0,
          "description": "Points between two `root_first` palettes; `30` keeps the whole\nladder under the word bonus (300) and the tier spread (150).",
          "format": "double",
          "type": "number"
        },
        "search_engine": {
          "default": "https://www.google.com/search?q={query}",
          "description": "The \"Search the web\" fallback's URL, `{query}` percent-encoded into it.",
          "type": "string"
        },
        "search_history": {
          "default": true,
          "description": "Remember the last 20 root queries that led to a pick (never synced:\n`frecency.json` in the profile). Up at the top of an empty root\nlist walks them; \"Clear Search History\" in pal's commands empties\nthem. `false` neither records nor recalls.",
          "type": "boolean"
        },
        "selection_snapshot": {
          "default": true,
          "description": "When an app does not expose its selected text to the accessibility\nAPI (`selection.text()`, `{selection}` in a snippet), fall back to\nsending the copy shortcut and reading the clipboard, which is put\nback as it was. `false` keeps pal off the clipboard: the selection\nis then only what the API reports. macOS needs Accessibility for\neither.",
          "type": "boolean"
        },
        "theme": {
          "$ref": "#/$defs/Theme",
          "default": "system"
        },
        "theme_file": {
          "default": "",
          "description": "A theme file overriding pal's colours, radii and fonts\n(`pal_core::theme`, docs/config.md \"Theme file\"): a name, looked up\nas `<config dir>/themes/<name>.toml`, or a path (`~` expanded).\nLight and dark sections apply to the scheme `theme` (or the OS)\npicks; the file is watched and applies live. Empty is pal's own.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "Hotkeys": {
      "anyOf": [
        {
          "description": "`hotkey = \"ctrl+space\"`; `\"\"` is off.",
          "type": "string"
        },
        {
          "description": "`hotkey = [\"cmd+space\", \"ctrl+space\"]`; `[]` is off.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      ],
      "description": "`general.hotkey`: one combination, or a list that all show the panel.\nThe two spellings are one setting; each is written back as it was read\n(the file's shape is kept until an edit needs the other one)."
    },
    "Instance": {
      "additionalProperties": false,
      "description": "`[instances.<key>]`: one configured copy of a `multi` extension. The\nkey is fixed at creation (`<name>@<suffix>`, `instance::is_key`); the\nbare name titles the default instance. Its settings live in\n`[extensions.<key>]`, its palettes' in `[palettes.\"<key>-<palette>\"]`,\nits bar items' in `[bar.items.\"<key>/<id>\"]`.",
      "properties": {
        "badge": {
          "description": "One or two characters in the tile's corner: the title's first\nletter when unset.",
          "type": "string"
        },
        "enabled": {
          "default": true,
          "description": "`false` parks the instance: not loaded, its rows and bar items gone,\nits settings kept.",
          "type": "boolean"
        },
        "tint": {
          "description": "The tile's colour, one of the twelve brand names (`red`, `amber`,\n`blue`, ...): picked from the suffix when unset. The default\ninstance keeps the extension's own tile.",
          "type": "string"
        },
        "title": {
          "description": "The display name (\"Work\"): the suffix capitalised when unset. The\ndefault instance has none until you name it.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "Palette": {
      "additionalProperties": false,
      "description": "Per-palette settings pal provides to every palette extension without it\ndeclaring them. A palette absent from the file gets the defaults.",
      "properties": {
        "alias": {
          "description": "An extra keyword on the palette's row at the root, so typing it finds\nthe palette; `Enter` then opens it as usual.",
          "type": "string"
        },
        "enabled": {
          "default": true,
          "type": "boolean"
        },
        "hotkey": {
          "description": "Hotkey that opens pal directly in this palette.",
          "type": "string"
        },
        "icon": {
          "description": "Icon override; the extension's own icon when unset.",
          "type": "string"
        },
        "item_hotkeys": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Global hotkeys that run one of the palette's items without showing\nthe panel, keyed by item id: `left_half = \"ctrl+alt+left\"` under\n`[palettes.window-management.item_hotkeys]`. The item's primary\naction runs as if picked.",
          "type": "object"
        },
        "settings": {
          "additionalProperties": true,
          "default": {},
          "description": "Settings the extension declared for this palette.",
          "type": "object"
        },
        "tier": {
          "anyOf": [
            {
              "$ref": "#/$defs/Tier"
            },
            {
              "type": "null"
            }
          ],
          "description": "The palette's tier at the root over what its manifest says:\n`primary` (reached by name, ranked up), `normal`, `catalog` (a big\nstatic list, ranked down and capped harder)."
        }
      },
      "type": "object"
    },
    "Position": {
      "description": "`top`: a fifth of the way down, where Spotlight and Raycast sit.\n`centre`: centred. `last`: wherever it was last shown.",
      "enum": [
        "top",
        "centre",
        "last"
      ],
      "type": "string"
    },
    "Theme": {
      "description": "Follow the OS, or force one.",
      "enum": [
        "system",
        "light",
        "dark"
      ],
      "type": "string"
    },
    "Tier": {
      "description": "A source's tier at the root: what its rows are to the user when typed\nfor next to everything else's. `primary` is what is reached by name\n(apps, windows, bookmarks); `normal` is what is browsed (containers,\npull requests); `catalog` is a big static list where any query matches\ndozens of rows (emoji, icons, unicode). A palette declares it in its\nmanifest, the config file can override it. See [`Tier::bonus`] for what\nit does to the score and [`Caps`] for the root's per-source cap.",
      "enum": [
        "primary",
        "normal",
        "catalog"
      ],
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "pal's settings: everything the settings view can set, in the file it\nwrites. (The set of keys is small and provisional; the mechanics around\nthem are what is settled.)",
  "properties": {
    "bar": {
      "$ref": "#/$defs/Bar",
      "default": {
        "hover_delay": 250,
        "hover_grace": 400,
        "items": {},
        "menubar": {
          "badge_style": "count",
          "dim": 50,
          "font": "system",
          "icon_size": 0.0,
          "max_chars": 32,
          "opacity": 100,
          "open_on_hover": false,
          "show_icon": true,
          "show_title": true,
          "size": 0.0,
          "spacing": 4,
          "text_size": 0.0,
          "urgent_color": "destructive",
          "width": 0
        },
        "sketchybar": {
          "badge_style": "count",
          "dim": 50,
          "font": "system",
          "icon_size": 0.0,
          "max_chars": 32,
          "opacity": 100,
          "open_on_hover": false,
          "position": "right",
          "show_icon": true,
          "show_title": true,
          "size": 0.0,
          "spacing": 4,
          "text_size": 0.0,
          "urgent_color": "destructive",
          "width": 0
        },
        "target": "auto"
      },
      "description": "Bar items: the menu bar and sketchybar strips (`docs/design/bar.md`)."
    },
    "extensions": {
      "additionalProperties": {
        "additionalProperties": true,
        "type": "object"
      },
      "default": {},
      "description": "Extension settings, keyed by extension name, or by instance key\n(`[extensions.\"gmail@work\"]`, which inherits `[extensions.gmail]`\nexcept its secrets and `scope: \"instance\"` settings). Shape is\nwhatever the extension declared.",
      "type": "object"
    },
    "general": {
      "$ref": "#/$defs/General",
      "default": {
        "alias_space": true,
        "ask_permissions_on_start": true,
        "backspace_back": true,
        "check_updates": true,
        "compact": false,
        "deeplink_confirm": true,
        "extension_dirs": [],
        "fallbacks": [
          "web",
          "url",
          "quicklinks",
          "calc",
          "files"
        ],
        "fallbacks_always": false,
        "hotkey": "ctrl+space",
        "launch_at_login": false,
        "menu_bar_icon": true,
        "now": [
          "calendar-today",
          "timer-timers",
          "media",
          "clipboard-rows"
        ],
        "pop_to_root": "after 90s",
        "position": "top",
        "root_caps": {
          "catalog": 3,
          "normal": 6,
          "primary": 8
        },
        "root_cut": true,
        "root_first": [
          "browser-tabs/tabs",
          "windows/windows",
          "apps/apps",
          "pal/palettes"
        ],
        "root_first_step": 30.0,
        "search_engine": "https://www.google.com/search?q={query}",
        "search_history": true,
        "selection_snapshot": true,
        "theme": "system",
        "theme_file": ""
      }
    },
    "instances": {
      "additionalProperties": {
        "$ref": "#/$defs/Instance"
      },
      "default": {},
      "description": "Instances of `multi` extensions, keyed `<name>@<suffix>`\n(`[instances.\"gmail@work\"]`; the table existing is what makes the\ninstance) or by the bare name to title the default one\n(`docs/design/instances.md`).",
      "type": "object"
    },
    "palettes": {
      "additionalProperties": {
        "$ref": "#/$defs/Palette"
      },
      "default": {},
      "description": "Per-palette settings, keyed by palette id.",
      "type": "object"
    },
    "states": {
      "additionalProperties": {
        "$ref": "#/$defs/Decl"
      },
      "default": {},
      "description": "States: named variables bar items and extensions read\n(`[states.working] expr = \"hour >= 9 and hour < 18\"`;\n`docs/design/states.md`).",
      "type": "object"
    }
  },
  "title": "pal config",
  "type": "object",
  "x-taplo-info": {
    "authors": [
      "pal"
    ],
    "patterns": [
      "**/pal/config.toml"
    ]
  }
}
