{
  // Prose wraps at 80; a table row or a code line is as long as it has to be.
  "config": { "MD013": { "line_length": 80, "tables": false, "code_blocks": false } },
  "ignores": ["node_modules", "**/node_modules", "target", "app/dist"]
}
