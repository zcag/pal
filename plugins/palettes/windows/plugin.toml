name = "windows"
desc = "list and focus windows"
icon = "preferences-system-windows"
icon_utf = "󰖲"
version = "0.1"
command = ["run.sh"]
# Hidden from listings where the backend isn't installed.
requires = ["yabai|hyprctl|swaymsg|wmctrl"]
