name = "systemd"
desc = "list and manage systemd services"
icon = "system-run"
icon_utf = "󰜎"
version = "0.1"
command = ["run.sh"]
# Hidden from listings where the backend isn't installed.
requires = ["systemctl"]
