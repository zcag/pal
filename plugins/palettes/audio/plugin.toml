name = "audio"
desc = "list and switch audio output devices"
icon = "audio-card"
icon_utf = "󰓃"
version = "0.1"
command = ["run.sh"]
# Hidden from listings where no backend is installed - PipeWire/PulseAudio, or SwitchAudioSource on macOS.
requires = ["pactl|wpctl|SwitchAudioSource"]
