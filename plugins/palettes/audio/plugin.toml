name = "audio"
desc = "list and switch audio output devices"
icon = "audio-card"
icon_utf = "󰓃"
version = "0.1"
command = ["run.sh"]
# Hidden from listings where the backend isn't installed - PipeWire/PulseAudio; a macOS port would add SwitchAudioSource here.
requires = ["pactl|wpctl"]
