name = "ble"
desc = "list and connect bluetooth devices"
icon = "bluetooth"
icon_utf = "󰂯"
version = "0.1"
command = ["run.sh"]
# Hidden from listings where the backend isn't installed - BlueZ; blueutil is the macOS equivalent.
requires = ["bluetoothctl"]
