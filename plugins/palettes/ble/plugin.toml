name = "ble"
desc = "list and connect bluetooth devices"
icon = "bluetooth"
icon_utf = "󰂯"
version = "0.1"
command = ["run.sh"]
# Hidden from listings where no backend is installed - BlueZ, or blueutil on macOS.
requires = ["bluetoothctl|blueutil"]
