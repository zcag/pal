name = "clipboard"
desc = "clipboard history"
icon = "edit-paste"
icon_utf = "󰆒"
version = "0.1"
command = ["run.sh"]
# Hidden from listings where the backend isn't installed - a Wayland clipboard daemon; on macOS Raycast owns this.
requires = ["cliphist|clipman"]
