name = "power"
desc = "power options"
icon = "system-shutdown"
icon_utf = "󰐥"
version = "0.1"
auto_list = true
auto_pick = true
default_action = "cmd"
action_key = "cmd"
# Hidden from listings where the backend isn't installed - the data file shells out to loginctl/systemctl.
requires = ["systemctl"]
