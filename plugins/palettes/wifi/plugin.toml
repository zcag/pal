name = "wifi"
desc = "list and connect to wifi networks"
icon = "network-wireless"
icon_utf = "󰖩"
version = "0.1"
command = ["run.sh"]
# Hidden from listings where the backend isn't installed - NetworkManager; networksetup is the macOS equivalent.
requires = ["nmcli"]
