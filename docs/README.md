# pal docs

- [Getting started](getting-started.md): install to the first extension,
  in the order things come up: permissions, the hotkey, the panel, the
  root, the bar, Settings, the config file, the store, the CLI.
- [Config](config.md): the config file key by key with its default:
  `[general]`, the theme file, `[palettes]`, `[bar]`, `[extensions]`,
  `[instances]`, secrets, live reload, diagnostics, profiles.
- [Palettes](palettes.md): every bundled palette with its keys, actions,
  settings and links.
- [Scripts and data files](scripts.md): the zero-code tier, a palette from
  a data file or a shell script, and script commands.
- [Keyboard](keyboard.md): the keyboard grammar, the shell's shortcuts,
  the other windows.
- [CLI](cli.md): `pal` and its subcommands, the store, instances, the
  bar, `pal pick`.
- [Links](links.md): `pal://` links and their `pal` twins, every route
  with an example, the extensions' own routes, what asks first.
- [Extensions](extensions.md): writing a palette in TypeScript: the
  manifest, the code, views, forms, bar items, links, the `@zcag/pal` API.
- [Troubleshooting](troubleshooting.md): the log, diagnostics,
  permissions, the hotkey, PATH, the extension host, a palette that lists
  nothing, Gatekeeper.
- [Releasing](releasing.md): how a release is cut and how the updater
  works.
